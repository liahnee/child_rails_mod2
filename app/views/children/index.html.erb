<h2> All Children's Contact </h2> 
Adoption maximum is 20! <br><br>

<% if params[:notice] %>
    <%= params[:notice] %>
<% end %>

<ul>
<% if @current_user.children.size > 0 %>
    <% @current_user.children.each do |child| %>
        <li>
        <b><%= link_to child.full_name, user_child_path(@current_user, child) %></b>
        </li>
        Age: <%= child.age_i %>  <br>
        Season: <%= child.season %><br><br>
    <% end %>
<% else %>
        <label>You currently have no adopted children.</label>
<% end %>
</ul>